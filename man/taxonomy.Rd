% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{taxonomy}
\alias{taxonomy}
\alias{deduplicate_taxonomy}
\alias{aggregate_taxa}
\title{Taxonomy helpers for microbiome measurements}
\usage{
deduplicate_taxonomy(taxa, na.value = "Unclassified", sep = "_")

aggregate_taxa(
  tab,
  taxa,
  id.col,
  normalize = TRUE,
  wide = FALSE,
  na.value = "Unclassified"
)
}
\arguments{
\item{taxa}{Output of \code{\link{otus}} or \code{\link{sgbs}}}

\item{na.value}{Name for missing levels}

\item{sep}{Separator for prefixed names}

\item{tab}{Long measurements with \code{sample_id}, an id column matching
\code{taxa}, and \code{value}}

\item{id.col}{Taxon id column shared by \code{tab} and \code{taxa}}

\item{normalize}{Per-sample proportions}

\item{wide}{Return samples x taxa matrices}
}
\value{
\code{deduplicate_taxonomy}: taxa table; \code{aggregate_taxa}:
  named list, one element per level
}
\description{
\code{deduplicate_taxonomy} makes taxon names unique at each level by
prefixing ancestors where the same name occurs under different parents;
\code{aggregate_taxa} sums measurements to each taxonomic level
(optionally normalizing each sample to proportions). Work for OTUs
(\code{otu_*} columns) and SGBs (\code{sgb_*} columns).
}
