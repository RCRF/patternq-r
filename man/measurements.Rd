% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/queries_measurements.R
\name{measurements}
\alias{measurements}
\alias{measurements_query}
\title{Measurement values of one type}
\usage{
measurements(
  measurement,
  measurement.set = NULL,
  db = NULL,
  samples = NULL,
  targets = NULL,
  wide = FALSE,
  fun.aggregate = mean,
  ...
)

measurements_query(
  measurement,
  measurement.set = NULL,
  samples = NULL,
  targets = character(0)
)
}
\arguments{
\item{measurement}{Measurement attribute without namespace}

\item{measurement.set}{Measurement set name. Required unless \code{targets}
is given (targets are detected per measurement set).}

\item{db}{Database name (defaults to \code{\link{current_db}})}

\item{samples}{Optional sample ids}

\item{targets}{Optional target reference attributes, e.g.
\code{c(":measurement/cell-population", ":measurement/epitope")}; use
\code{character(0)} for none}

\item{wide}{Return a samples x targets matrix (see \code{\link{to_matrix}})
instead of long format}

\item{fun.aggregate}{Aggregation for duplicate sample/target cells when
\code{wide = TRUE}}

\item{...}{Passed to \code{\link{do_query}}}
}
\value{
Long format: \code{sample_id}, \code{measurement_set}, target
  column(s), \code{value}; or a matrix when \code{wide = TRUE}
}
\description{
Generic retrieval of any measurement attribute (e.g. \code{"tpm"},
\code{"percent-of-parent"}, \code{"olink-npx"}, \code{"median-channel-value"})
with what each measurement is of (its target: gene, cell population,
epitope, variant, ...). Targets are detected from the measurement set's
measurements unless given.
}
