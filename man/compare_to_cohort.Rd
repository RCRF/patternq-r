% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_expression.R
\name{compare_to_cohort}
\alias{compare_to_cohort}
\title{Compare a sample's expression to a reference cohort}
\usage{
compare_to_cohort(
  sample,
  db = NULL,
  cohort.db,
  measurement = "tpm",
  cohort.measurement = measurement,
  genes = NULL,
  measurement.set = NULL,
  cohort.measurement.set = NULL,
  log = TRUE,
  fill.missing = TRUE,
  anchor.gene = "GAPDH",
  sd.floor = 0.25,
  batch.size = 2000,
  min.cohort = 5
)
}
\arguments{
\item{sample}{Sample id in \code{db}}

\item{db}{Database holding the sample}

\item{cohort.db}{Reference cohort database, e.g. \code{resolve_db("tcga-uvm")}}

\item{measurement}{Measurement attribute for the sample}

\item{cohort.measurement}{Measurement attribute for the cohort (defaults to
\code{measurement})}

\item{genes}{Genes to compare (default: all genes measured in the sample)}

\item{measurement.set, cohort.measurement.set}{Optional measurement sets}

\item{log}{Compare on \code{log2(1 + x)}}

\item{fill.missing}{Count a gene with no stored value in a cohort sample as
0. Imports often omit zero measurements, so without this a gene's cohort
distribution is built from only the samples that express it. Cohort
samples are those with a value for \code{anchor.gene}.}

\item{anchor.gene}{Housekeeping gene that defines the cohort's samples}

\item{sd.floor}{Minimum cohort standard deviation (on the comparison scale)
so near-constant genes do not produce enormous z-scores}

\item{batch.size}{Genes per cohort query}

\item{min.cohort}{Minimum cohort values for a gene to be scored}
}
\value{
\code{data.frame}: \code{hgnc_symbol}, \code{value}, \code{z},
  \code{percentile}, \code{cohort_n} (cohort size), \code{cohort_observed}
  (cohort samples with a stored value; low coverage of a gene that is
  usually expressed points to an import or annotation problem),
  \code{cohort_mean}, \code{cohort_sd}, \code{cohort_median}
}
\description{
For every gene, the sample's value is placed within the cohort's
distribution: cohort mean/sd/median on \code{log2(1 + x)} scale (or raw with
\code{log = FALSE}), z-score, and percentile rank. The cohort is streamed in
gene batches, so a whole transcriptome against a large cohort never needs to
be held in memory.
}
\details{
Measurements must be comparable (same units / normalization) between the
sample's database and the cohort's; TPM is the usual common ground.
}
